{
  "description": "Same clean/ready/elapsed state as 64, but the repository requires a merge queue (isMergeQueueEnabled) and the PR is not in it yet. buildReadyMergeResult reads mergeRequirements.mergeQueue.required and emits a queue-mode command plus a GraphQL enqueue fallback instead of an ordinary auto-merge command.",
  "args": ["--merge"],
  "batchData": {
    "isMergeQueueEnabled": true,
    "checks": [
      {
        "name": "CI / tests",
        "status": "COMPLETED",
        "conclusion": "SUCCESS",
        "event": "pull_request",
        "detailsUrl": "https://github.com/owner/repo/actions/runs/100",
        "runId": "100"
      }
    ]
  },
  "readyDelayState": {
    "isReady": true,
    "shouldCancel": true,
    "remainingSeconds": 0
  },
  "expectedExitCode": 15
}
